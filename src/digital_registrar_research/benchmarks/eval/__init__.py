"""Canonical scoring harness — the single source of truth for metrics + scope.

Both `benchmarks.run_all` and `ablations.run_ablations` score against this module
so numbers are directly comparable across the two studies.
"""
