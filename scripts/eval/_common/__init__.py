"""Shared utilities for the eval subcommands.

Path resolution, argument parsing, three-way outcome classification,
JSON loaders, organ stratification, manifest reporting, and statistical
helpers not present in ``src/.../eval/``.
"""
