"""Diagnostics subcommand: source-of-error decomposition, difficulty
tier stratification, worst-cases catalog. Reads pre-computed outputs
from ``non_nested``, ``nested`` and ``iaa`` and produces the
reviewer-rebuttal CSVs.
"""
